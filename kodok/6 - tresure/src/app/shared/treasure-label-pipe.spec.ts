import { TreasureLabelPipe } from './treasure-label-pipe';

describe('TreasureLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new TreasureLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
