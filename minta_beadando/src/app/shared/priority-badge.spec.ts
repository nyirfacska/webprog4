import { PriorityBadge } from './priority-badge';

describe('PriorityBadge', () => {
  it('should create an instance', () => {
    const directive = new PriorityBadge();
    expect(directive).toBeTruthy();
  });
});
